import React from "react";
import skills from "../../const/skills";

type Props = {
  // Any of the skills in the skills object
};

export default function ShopForSkillContainer({}: Props) {
  return <div>ShopForSkillContainer</div>;
}
